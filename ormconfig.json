{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "productsapi",
  "migrations": [
    "./shared/infra/typeorm/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./shared/infra/typeorm/migrations"
  }
}
